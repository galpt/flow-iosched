From 16815029df859db4e4e6ba246f2ca25e4b92135f Mon Sep 17 00:00:00 2001
From: Galih Tama <galpt@v.recipes>
Date: Thu, 14 May 2026 20:18:06 +0700
Subject: [PATCH] block: add Multi-Lane I/O Scheduler (FLOW) v2.0.0

Add the Multi-Lane I/O scheduler (flow-iosched), a blk-mq elevator with
five priority tiers adapted from the scx_flow CPU scheduling design.

Version 2.0.0 brings the scheduler up to parity with scx_flow's lane
architecture: proportional budget refill on I/O completion, lock-free
atomic per-ICQ access, bounded geometric signal decay, latency
credit/debt tracking, per-process IO profiles, a starvation quota
mechanism, per-hctx metrics, and a 3-mode autotuner.

Signed-off-by: Galih Tama <galpt@v.recipes>
---
 block/Kconfig.iosched |   25 +
 block/Makefile        |    1 +
 block/flow-iosched.c  | 1767 +++++++++++++++++++++++++++++++++++++++++
 3 files changed, 1793 insertions(+)

diff --git a/block/Kconfig.iosched b/block/Kconfig.iosched
index 27f1132..4d227d8 100644
--- a/block/Kconfig.iosched
+++ b/block/Kconfig.iosched
@@ -44,4 +44,29 @@ config BFQ_CGROUP_DEBUG
 	Enable some debugging help. Currently it exports additional stat
 	files in a cgroup which can be useful for debugging.
 
+
+config MQ_IOSCHED_FLOW
+	tristate "Multi-Lane I/O scheduler (FLOW)"
+	default m
+	help
+	  The Multi-Lane I/O scheduler (FLOW) is a blk-mq I/O scheduler with
+	  five priority tiers adapted from the scx_flow CPU scheduling design.
+	  It provides deadline-sorted dispatch, per-process I/O budget
+	  containment, and starvation-safe batching for modern multi-queue
+	  block devices.
+
+	  Tier 0 (Emergency):  Unconditional bypass for BLK_MQ_INSERT_AT_HEAD
+	  Tier 1 (Reserved):   Low-latency path for synchronous reads/metadata
+	  Tier 2 (Latency):    Bounded responsiveness for small random I/O
+	  Tier 3 (Shared):     Background throughput for async writes
+	  Tier 4 (Contained):  Throttled path for I/O hogs
+
+config MQ_IOSCHED_DEFAULT_FLOW
+	bool "Enable FLOW I/O scheduler as the default MQ scheduler"
+	depends on MQ_IOSCHED_FLOW=y
+	default n
+	help
+	  Enable the FLOW I/O scheduler as the default blk-mq I/O scheduler.
+	  The elevator core will automatically select "flow-iosched" for
+	  multi-queue block devices during boot.
 endmenu
diff --git a/block/Makefile b/block/Makefile
index c65f4da..bc27a56 100644
--- a/block/Makefile
+++ b/block/Makefile
@@ -24,6 +24,7 @@ obj-$(CONFIG_MQ_IOSCHED_DEADLINE)	+= mq-deadline.o
 obj-$(CONFIG_MQ_IOSCHED_KYBER)	+= kyber-iosched.o
 bfq-y				:= bfq-iosched.o bfq-wf2q.o bfq-cgroup.o
 obj-$(CONFIG_IOSCHED_BFQ)	+= bfq.o
+obj-$(CONFIG_MQ_IOSCHED_FLOW)	+= flow-iosched.o
 
 obj-$(CONFIG_BLK_DEV_INTEGRITY) += bio-integrity.o blk-integrity.o t10-pi.o \
 				   bio-integrity-auto.o
diff --git a/block/flow-iosched.c b/block/flow-iosched.c
new file mode 100644
index 0000000..6877056
--- /dev/null
+++ b/block/flow-iosched.c
@@ -0,0 +1,1773 @@
+// SPDX-License-Identifier: GPL-2.0
+/*
+ * Multi-Lane I/O Scheduler (FLOW) — v2.0.0
+ *
+ * A multi-lane I/O scheduler adapted from the scx_flow CPU scheduling design.
+ * Five priority tiers, deadline-sorted rbtree dispatch, per-process budget
+ * containment with proportional completion-based refill, lock-free atomic
+ * per-ICQ access, bounded geometric signal decay (latency credit/debt),
+ * per-process IO profiles, starvation quota mechanism, per-hctx metrics,
+ * and a 3-mode autotuner.
+ *
+ * Tier 0 - Emergency:  prio_queue[0], unconditional FIFO bypass
+ * Tier 1 - Reserved:   deadline-sorted rbtree, sync reads + REQ_META/PRIO
+ * Tier 2 - Latency:    deadline-sorted rbtree, small/REQ_SYNC writes
+ * Tier 3 - Shared:     deadline-sorted rbtree, async best-effort
+ * Tier 4 - Contained:  deadline-sorted rbtree, hog-throttled
+ *
+ * Dispatch order: Emergency > Reserved > Latency > Shared > Contained
+ */
+
+#include <linux/bio.h>
+#include <linux/blkdev.h>
+#include <linux/blk-mq.h>
+#include <linux/compiler.h>
+#include "elevator.h"	/* block/elevator.h — in-tree; for out-of-tree use <linux/elevator.h> */
+#include <linux/fs.h>
+#include <linux/init.h>
+#include <linux/kernel.h>
+#include <linux/math.h>
+#include <linux/mempool.h>
+#include <linux/module.h>
+#include <linux/rbtree.h>
+#include <linux/sbitmap.h>
+#include <linux/slab.h>
+#include <linux/timekeeping.h>
+#include <linux/timer.h>
+#include <linux/atomic.h>
+#include <linux/version.h>
+
+/*
+ * Kernel 6.15 renamed del_timer_sync() to timer_delete_sync().
+ * Kernels >= 6.15 only have timer_delete_sync; older kernels (6.12-6.14)
+ * only have del_timer_sync.  We use del_timer_sync in the source and
+ * provide a compat define for newer kernels.
+ *
+ * The 0002 compat patch (6.12-6.17) adds extra handling for the
+ * older init_sched API; the timer API boundary at 6.15 is orthogonal.
+ */
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 15, 0)
+#define del_timer_sync timer_delete_sync
+#endif
+
+#include "blk.h"
+#include "blk-mq.h"
+#include "blk-mq-sched.h"
+
+#define FLOW_VERSION "2.0.0"
+
+/* ── Autotune modes ──────────────────────────────────────────────── */
+#define FLOW_AUTOTUNE_BALANCED		0U
+#define FLOW_AUTOTUNE_LATENCY		1U
+#define FLOW_AUTOTUNE_THROUGHPUT	2U
+
+/* ── Lane identifiers ─────────────────────────────────────────────── */
+enum flow_lane {
+	FLOW_LANE_EMERGENCY	= 0,
+	FLOW_LANE_RESERVED	= 1,
+	FLOW_LANE_LATENCY	= 2,
+	FLOW_LANE_SHARED	= 3,
+	FLOW_LANE_CONTAINED	= 4,
+	FLOW_NR_LANES		= 5,
+};
+
+/* ── Operation types for batch queues ─────────────────────────────── */
+enum flow_optype {
+	FLOW_READ	= 0,
+	FLOW_WRITE	= 1,
+	FLOW_DISCARD	= 2,
+	FLOW_OTHER	= 3,
+	FLOW_NR_OPTYPES	= 4,
+};
+
+/* ─── Scheduling thresholds ───────────────────────────────────────── */
+#define FLOW_QUANTUM_SHIFT		20
+#define FLOW_STARVATION_MAX_DEFAULT_RESERVED	5U
+#define FLOW_STARVATION_MAX_DEFAULT_SHARED	20U
+#define FLOW_STARVATION_MAX_DEFAULT_CONTAINED	30U
+#define FLOW_CONTAIN_THRESHOLD_DEFAULT		100U
+#define FLOW_CONTAIN_DECAY_STEP_DEFAULT		8U
+#define FLOW_BUDGET_REFRESH_NS			100000000ULL  /* 100 ms */
+#define FLOW_BUDGET_SECTORS_DEFAULT_SYNC	2048
+#define FLOW_BUDGET_SECTORS_DEFAULT_ASYNC	512
+#define FLOW_BATCH_MAX_READ_DEFAULT		16
+#define FLOW_BATCH_MAX_WRITE_DEFAULT		16
+#define FLOW_COMPLETION_WINDOW_NS_DEFAULT	8000000ULL
+#define FLOW_MAX_INSERTS_PER_LOCK		72
+#define FLOW_MAX_DELETES_PER_LOCK		24
+
+/* ── Containment scoring ──────────────────────────────────────────── */
+#define FLOW_SCORE_INC_AT_EXHAUST		10U
+#define FLOW_SCORE_DECAY_PER_REFILL		1U
+
+/* ── Shared queue depth throttle ratio ────────────────────────────── */
+#define FLOW_ASYNC_DEPTH_RATIO			3
+
+/* ── Bounded signal limits (ported from scx_flow) ────────────────── */
+#define FLOW_HOG_SCORE_MAX			100U
+#define FLOW_HOG_SCORE_DECAY_SHIFT		3U
+#define FLOW_LATENCY_CREDIT_MAX			4U
+#define FLOW_LATENCY_CREDIT_DECAY_SHIFT		1U
+#define FLOW_LATENCY_DEBT_MAX			4U
+#define FLOW_LATENCY_DEBT_DECAY_SHIFT		1U
+#define FLOW_LATENCY_DEBT_URGENT_DEFAULT	1U
+#define FLOW_REFILL_DIV				100U
+#define FLOW_HIGH_PRIORITY_QUOTA_MAX_DEFAULT	4U
+#define FLOW_RESERVED_LANE_BURST_MAX_DEFAULT	5U
+
+/* ── Autotune defaults ───────────────────────────────────────────── */
+#define FLOW_AUTOTUNE_INTERVAL_HZ		1U
+
+/* ── IO profile bits (adapted from scx_flow wake_profile) ────────── */
+enum flow_io_profile {
+	IO_PROFILE_POSITIVE_BUDGET	= 1U << 0,
+	IO_PROFILE_CONTAINED		= 1U << 1,
+	IO_PROFILE_LATENCY_ALLOWANCE	= 1U << 2,
+	IO_PROFILE_LATENCY_PRESSURE	= 1U << 3,
+	IO_PROFILE_LATENCY_LANE		= 1U << 4,
+	IO_PROFILE_URGENT_LATENCY	= 1U << 5,
+	IO_PROFILE_SYNC_DOMINANT	= 1U << 6,
+};
+
+/* ── Per-request scheduler data ───────────────────────────────────── */
+struct flow_rq_data {
+	struct list_head	dl_node;	/* node in dl_group->rqs */
+	struct dl_group		*dl_group;	/* back-pointer to owning dl_group */
+	struct request		*rq;		/* back-pointer */
+	u64			deadline;	/* absolute deadline */
+	u32			block_size;	/* blk_rq_bytes(rq) */
+	u8			lane;		/* lane at insert time */
+};
+
+/* ── Deadline group (same quantised deadline, one rbtree node) ────── */
+struct dl_group {
+	struct rb_node		node;
+	struct list_head	rqs;		/* list of flow_rq_data->dl_node */
+	u64			deadline;	/* quantised deadline (rbtree key) */
+	u8			lane;		/* lane identifier */
+};
+
+/* ── Per-io_cq data (budget + containment + signal state) ────────── */
+struct flow_icq_data {
+	atomic64_t	io_budget_sectors;	/* atomic — remaining I/O budget */
+	atomic_t	containment_score;	/* atomic — hog score 0..FLOW_HOG_SCORE_MAX */
+	atomic_t	latency_allowance;	/* atomic — credit for latency lane 0..4 */
+	atomic_t	latency_pressure;	/* atomic — debt for urgency 0..4 */
+	atomic_t	io_profile;		/* atomic — profile bits (enum flow_io_profile) */
+	atomic64_t	last_io_completed;	/* last completion timestamp */
+	atomic64_t	last_io_inserted;	/* last insertion timestamp */
+	atomic64_t	last_io_profile_update;	/* last profile recompute timestamp */
+};
+
+/* ── Per-request-queue scheduler data ─────────────────────────────── */
+struct flow_data {
+	/* Priority queues (Tier 0) */
+	struct list_head	prio_queue[2];
+
+	/* Deadline trees for tiers 1-4 */
+	struct rb_root_cached	reserved_root;
+	struct rb_root_cached	latency_root;
+	struct rb_root_cached	shared_root;
+	struct rb_root_cached	contained_root;
+
+	/* Sector-sorted merge rbtree (uses rq->rb_node) */
+	struct rb_root_cached	merge_root;
+
+	/* Budget defaults */
+	s32			sync_budget_sectors;
+	s32			async_budget_sectors;
+
+	/* Containment */
+	u32			contain_threshold;
+	u32			contain_decay_step;
+
+	/* Starvation thresholds (global sysfs tunables) */
+	u32			starvation_max[FLOW_NR_LANES];
+
+	/* Batching parameters */
+	u64			completion_window_ns;
+	u16			batch_max_read;
+	u16			batch_max_write;
+
+	/* Read priority bias */
+	s8			read_priority;
+
+	/* Memory pools */
+	mempool_t		*rq_data_pool;
+	struct kmem_cache	*rq_data_cache;
+	struct kmem_cache	*dl_group_cache;
+
+	/* Queue properties */
+	bool			is_rotational;
+
+	/* Dispatch cursor */
+	sector_t		head_pos;
+
+	/* Back-pointer to request queue (for blk_stat) */
+	struct request_queue	*queue;
+
+	/* Spinlock */
+	spinlock_t		lock;
+
+	/* ── Autotune state ────────────────────────────────────── */
+	u32			autotune_mode;		/* FLOW_AUTOTUNE_* */
+	u32			autotune_generation;
+	struct timer_list	autotune_timer;
+
+	/* ── Tuned parameters (overridden by autotune) ─────────── */
+	u32			tuned_starvation_max[FLOW_NR_LANES];
+	u32			high_priority_quota_max;
+	u32			reserved_lane_burst_max;
+};
+
+/* ── Per-hardware-context scheduler data ──────────────────────────── */
+struct flow_hctx_data {
+	spinlock_t		lock;
+	struct list_head	dispatch_list;	/* requests ready to dispatch from this hctx */
+
+	/* Per-hctx starvation tracking */
+	u32			starvation_rounds[FLOW_NR_LANES];
+
+	/* Quota burst counter — incremented on high-priority dispatch, reset on lower lanes */
+	u32			high_priority_burst_rounds;
+
+	/* Back-pointer */
+	struct blk_mq_hw_ctx	*hctx;
+
+	/* Per-hctx metrics (atomic64_t — written under fd->lock, read under khd->lock) */
+	atomic64_t		m_reserved_dispatch;
+	atomic64_t		m_latency_dispatch;
+	atomic64_t		m_shared_dispatch;
+	atomic64_t		m_contained_dispatch;
+	atomic64_t		m_contained_rescue;
+	atomic64_t		m_shared_rescue;
+	atomic64_t		m_budget_exhaustions;
+	atomic64_t		m_latency_debt_raises;
+	atomic64_t		m_hog_containments;
+};
+
+/* ── Helpers ──────────────────────────────────────────────────────── */
+
+static inline struct flow_rq_data *get_rq_data(struct request *rq)
+{
+	return rq->elv.priv[0];
+}
+
+static inline struct flow_icq_data *icq_to_flow_icq(struct io_cq *icq)
+{
+	if (!icq)
+		return NULL;
+	return (struct flow_icq_data *)(icq + 1);
+}
+
+static inline u8 flow_optype(struct request *rq)
+{
+	switch (req_op(rq)) {
+	case REQ_OP_READ:
+		return FLOW_READ;
+	case REQ_OP_WRITE:
+		return FLOW_WRITE;
+	case REQ_OP_DISCARD:
+		return FLOW_DISCARD;
+	default:
+		return FLOW_OTHER;
+	}
+}
+
+static inline bool rq_is_sync_read(struct request *rq)
+{
+	return (req_op(rq) == REQ_OP_READ) && (rq->cmd_flags & REQ_SYNC);
+}
+
+static inline u64 flow_deadline(struct request *rq, u8 lane)
+{
+	/* Tier 1 (Reserved): FIFO-ish using start_time */
+	if (lane == FLOW_LANE_RESERVED)
+		return rq->start_time_ns;
+
+	/* Tier 2 (Latency): small I/O, use start_time + short window */
+	if (lane == FLOW_LANE_LATENCY)
+		return rq->start_time_ns + (2ULL * NSEC_PER_MSEC);
+
+	/* Tier 3 (Shared): async, use start_time + large window */
+	if (lane == FLOW_LANE_SHARED)
+		return rq->start_time_ns + (2000ULL * NSEC_PER_MSEC);
+
+	/* Tier 4 (Contained): furthest deadline */
+	return rq->start_time_ns + (5000ULL * NSEC_PER_MSEC);
+}
+
+/* ── Bounded signal helpers (ported from scx_flow) ──────────────── */
+
+static inline u32 flow_raise_bounded(u32 signal, u32 delta, u32 signal_max)
+{
+	if (!delta)
+		return signal;
+	u32 sum = signal + delta;
+	return sum > signal_max ? signal_max : sum;
+}
+
+static inline u32 flow_decay_bounded(u32 signal, u32 delta)
+{
+	if (!delta || !signal)
+		return signal;
+	return signal <= delta ? 0 : signal - delta;
+}
+
+static inline u32 flow_decay_geometric(u32 signal, u32 shift)
+{
+	if (!signal)
+		return 0;
+	u32 d = shift ? signal >> shift : 1;
+	if (!d)
+		d = 1;
+	return signal > d ? signal - d : 0;
+}
+
+static inline u32 flow_decay_confidence(u32 signal, u32 delta, u32 shift)
+{
+	u32 s = flow_decay_geometric(signal, shift);
+	if (delta > 1)
+		s = flow_decay_bounded(s, delta - 1);
+	return s;
+}
+
+/* Atomic variants for concurrent per-ICQ access */
+static inline u32 flow_raise_bounded_atomic(atomic_t *v, u32 delta, u32 sig_max)
+{
+	u32 old, new;
+
+	do {
+		old = atomic_read(v);
+		new = flow_raise_bounded(old, delta, sig_max);
+	} while (atomic_cmpxchg(v, old, new) != old);
+	return new;
+}
+
+static inline u32 flow_decay_geometric_atomic(atomic_t *v, u32 shift)
+{
+	u32 old, new;
+
+	do {
+		old = atomic_read(v);
+		new = flow_decay_geometric(old, shift);
+	} while (atomic_cmpxchg(v, old, new) != old);
+	return new;
+}
+
+/* ── IO profile recomputation (adapted from scx_flow recompute_wake_profile) ── */
+
+static inline bool flow_has_urgent_pressure(const struct flow_icq_data *ficq,
+					    u32 debt_urgent_min)
+{
+	u32 pressure = atomic_read(&ficq->latency_pressure);
+	return pressure >= debt_urgent_min;
+}
+
+static void flow_recompute_io_profile(struct flow_icq_data *ficq,
+				      struct request *rq,
+				      struct flow_data *fd)
+{
+	u32 profile = 0;
+	s64 budget = atomic64_read(&ficq->io_budget_sectors);
+	u32 containment = atomic_read(&ficq->containment_score);
+	u32 allowance = atomic_read(&ficq->latency_allowance);
+	u32 pressure = atomic_read(&ficq->latency_pressure);
+	bool positive_budget = budget > 0;
+	bool contained = containment >= fd->contain_threshold;
+
+	if (positive_budget)
+		profile |= IO_PROFILE_POSITIVE_BUDGET;
+	if (contained)
+		profile |= IO_PROFILE_CONTAINED;
+	if (allowance > 0 && positive_budget)
+		profile |= IO_PROFILE_LATENCY_ALLOWANCE;
+	if (pressure > 0)
+		profile |= IO_PROFILE_LATENCY_PRESSURE;
+
+	bool allowance_ready = !contained && (profile & IO_PROFILE_LATENCY_ALLOWANCE);
+	bool pressure_ready = !contained && flow_has_urgent_pressure(ficq,
+					FLOW_LATENCY_DEBT_URGENT_DEFAULT);
+	if (allowance_ready || pressure_ready) {
+		profile |= IO_PROFILE_LATENCY_LANE;
+		if (pressure_ready)
+			profile |= IO_PROFILE_URGENT_LATENCY;
+	}
+
+	if (rq && (rq->cmd_flags & REQ_SYNC))
+		profile |= IO_PROFILE_SYNC_DOMINANT;
+
+	atomic_set(&ficq->io_profile, profile);
+	atomic64_set(&ficq->last_io_profile_update, ktime_get_ns());
+}
+
+/* ── Lane assignment ──────────────────────────────────────────────── */
+
+static u8 flow_assign_lane(struct request *rq, blk_insert_t insert_flags,
+			   struct flow_data *fd)
+{
+	/* Tier 0: Emergency - unconditional bypass */
+	if (insert_flags & BLK_MQ_INSERT_AT_HEAD)
+		return FLOW_LANE_EMERGENCY;
+
+	/* Tier 1: Reserved (sync reads, metadata, priority) */
+	if (rq_is_sync_read(rq) ||
+	    (rq->cmd_flags & (REQ_META | REQ_PRIO)))
+		return FLOW_LANE_RESERVED;
+
+	/*
+	 * Check IO profile for latency-sensitive classification
+	 * (adapted from scx_flow wake_profile-based lane selection).
+	 */
+	if (rq->elv.icq) {
+		struct flow_icq_data *ficq = icq_to_flow_icq(rq->elv.icq);
+		if (ficq) {
+			u32 profile = atomic_read(&ficq->io_profile);
+			if (profile & IO_PROFILE_URGENT_LATENCY)
+				return FLOW_LANE_LATENCY;
+		}
+	}
+
+	/* Tier 2: Latency (REQ_SYNC writes, small I/O ≤ 4 KB) */
+	if ((rq->cmd_flags & REQ_SYNC) || blk_rq_bytes(rq) <= 4096)
+		return FLOW_LANE_LATENCY;
+
+	/* Default: Tier 3 - Shared (async best-effort) */
+	return FLOW_LANE_SHARED;
+}
+
+/* ── Budget / containment (atomic, lock-free per-ICQ) ────────────── */
+
+/*
+ * Track budget exhaustion and containment events per-hctx.
+ * Called from flow_insert_request which has access to the hctx via insert_flags.
+ * We pass the hctx explicitly for metric accounting.
+ */
+static bool flow_check_budget_and_contain(struct flow_icq_data *ficq,
+					  struct request *rq,
+					  struct flow_data *fd,
+					  struct blk_mq_hw_ctx *hctx)
+{
+	u32 block_sectors = blk_rq_sectors(rq);
+	s64 budget;
+	s64 old_budget;
+
+	if (!ficq)
+		return false;
+
+	/* Get per-process budget, keyed by sync vs async */
+	budget = (rq->cmd_flags & REQ_SYNC) ?
+		  fd->sync_budget_sectors : fd->async_budget_sectors;
+
+	/* Clamp budget to safe range */
+	if (budget < 64)
+		budget = 64;
+	else if (budget > 65536)
+		budget = 65536;
+
+	/*
+	 * Refill if idle long enough (secondary safety valve).
+	 * Primary refill happens in flow_completed_request().
+	 */
+	old_budget = atomic64_read(&ficq->io_budget_sectors);
+	if (old_budget < budget &&
+	    ktime_get_ns() - atomic64_read(&ficq->last_io_completed) >
+		FLOW_BUDGET_REFRESH_NS) {
+		old_budget = atomic64_add_return(budget, &ficq->io_budget_sectors);
+		s64 cap = budget * 4;
+		if (old_budget > cap)
+			atomic64_set(&ficq->io_budget_sectors, cap);
+		flow_decay_geometric_atomic(&ficq->containment_score,
+					    FLOW_HOG_SCORE_DECAY_SHIFT);
+	}
+
+	atomic64_set(&ficq->last_io_inserted, ktime_get_ns());
+
+	/* Consume budget atomically */
+	old_budget = atomic64_sub_return((s64)block_sectors,
+					  &ficq->io_budget_sectors);
+
+	/* Raise containment score on exhaustion */
+	if (old_budget < 0) {
+		flow_raise_bounded_atomic(&ficq->containment_score,
+					  FLOW_SCORE_INC_AT_EXHAUST,
+					  FLOW_HOG_SCORE_MAX);
+		/* Track exhaustion for autotune metrics */
+		struct flow_hctx_data *khd = hctx->sched_data;
+		if (khd)
+			atomic64_inc(&khd->m_budget_exhaustions);
+	}
+
+	/* Return true if request should be contained */
+	return atomic_read(&ficq->containment_score) >= fd->contain_threshold;
+}
+
+/* ── DL tree operations ───────────────────────────────────────────── */
+
+static struct rb_root_cached *flow_root_for_lane(struct flow_data *fd, u8 lane)
+{
+	switch (lane) {
+	case FLOW_LANE_RESERVED:
+		return &fd->reserved_root;
+	case FLOW_LANE_LATENCY:
+		return &fd->latency_root;
+	case FLOW_LANE_SHARED:
+		return &fd->shared_root;
+	case FLOW_LANE_CONTAINED:
+		return &fd->contained_root;
+	default:
+		return NULL;
+	}
+}
+
+static bool flow_add_to_dl_tree(struct flow_data *fd, u8 lane,
+				struct request *rq)
+{
+	struct rb_root_cached *root = flow_root_for_lane(fd, lane);
+	struct rb_node **link = &root->rb_root.rb_node, *parent = NULL;
+	bool leftmost = true;
+	struct flow_rq_data *rd = get_rq_data(rq);
+	struct dl_group *dlg, *new_dlg;
+	u64 deadline;
+
+	lockdep_assert_held(&fd->lock);
+	if (!root || !rd)
+		return false;
+
+	rd->lane = lane;
+	rd->deadline = flow_deadline(rq, lane);
+	rd->block_size = blk_rq_bytes(rq);
+	rd->dl_group = NULL;
+
+	/* Quantise deadline for grouping */
+	deadline = rd->deadline & ~((1ULL << FLOW_QUANTUM_SHIFT) - 1);
+
+	/* Pre-allocate a dl_group node in case the walk falls through */
+	new_dlg = kmem_cache_zalloc(fd->dl_group_cache, GFP_ATOMIC);
+	if (!new_dlg)
+		return false;
+
+	while (*link) {
+		dlg = rb_entry(*link, struct dl_group, node);
+		parent = *link;
+		if ((s64)(deadline - dlg->deadline) < 0) {
+			link = &(*link)->rb_left;
+		} else if ((s64)(deadline - dlg->deadline) > 0) {
+			link = &(*link)->rb_right;
+			leftmost = false;
+		} else {
+			kmem_cache_free(fd->dl_group_cache, new_dlg);
+			goto found;
+		}
+	}
+
+	/* No existing node — use the pre-allocated slot */
+	dlg = new_dlg;
+	dlg->deadline = deadline;
+	dlg->lane = lane;
+	INIT_LIST_HEAD(&dlg->rqs);
+	rb_link_node(&dlg->node, parent, link);
+	rb_insert_color_cached(&dlg->node, root, leftmost);
+
+found:
+	rd->dl_group = dlg;
+	list_add_tail(&rd->dl_node, &dlg->rqs);
+	return true;
+}
+
+static void flow_del_from_dl_tree(struct flow_data *fd, u8 lane,
+				  struct request *rq)
+{
+	struct rb_root_cached *root = flow_root_for_lane(fd, lane);
+	struct flow_rq_data *rd = get_rq_data(rq);
+	struct dl_group *dlg;
+
+	lockdep_assert_held(&fd->lock);
+	if (!root || !rd || !rd->dl_group || list_empty(&rd->dl_node))
+		return;
+
+	dlg = rd->dl_group;	/* use stored back-pointer instead of container_of */
+	rd->dl_group = NULL;
+	list_del_init(&rd->dl_node);
+	if (list_empty(&dlg->rqs)) {
+		rb_erase_cached(&dlg->node, root);
+		kmem_cache_free(fd->dl_group_cache, dlg);
+	}
+}
+
+static struct flow_rq_data *flow_first_rq_for_lane(struct flow_data *fd,
+						   u8 lane)
+{
+	struct rb_root_cached *root = flow_root_for_lane(fd, lane);
+	struct rb_node *first;
+	struct dl_group *dlg;
+
+	if (!root)
+		return NULL;
+
+	first = rb_first_cached(root);
+	if (!first)
+		return NULL;
+
+	dlg = rb_entry(first, struct dl_group, node);
+	return list_first_entry_or_null(&dlg->rqs, struct flow_rq_data,
+					dl_node);
+}
+
+/* ── Merge rbtree helpers (sector-sorted, uses rq->rb_node) ──────── */
+
+static struct request *flow_former_request(struct request_queue *q,
+					   struct request *rq)
+{
+	struct rb_node *prev = rb_prev(&rq->rb_node);
+
+	if (prev)
+		return rb_entry_rq(prev);
+	return NULL;
+}
+
+static struct request *flow_next_request(struct request_queue *q,
+					 struct request *rq)
+{
+	struct rb_node *next = rb_next(&rq->rb_node);
+
+	if (next)
+		return rb_entry_rq(next);
+	return NULL;
+}
+
+static void flow_add_to_merge_rb(struct flow_data *fd, struct request *rq)
+{
+	elv_rb_add(&fd->merge_root.rb_root, rq);
+}
+
+static void flow_del_from_merge_rb(struct flow_data *fd, struct request *rq)
+{
+	if (!RB_EMPTY_NODE(&rq->rb_node))
+		elv_rb_del(&fd->merge_root.rb_root, rq);
+}
+
+/* ── Remove request from scheduler (dl_tree + merge_rb) ──────────── */
+
+static void flow_remove_request(struct flow_data *fd, struct request *rq,
+				struct request_queue *q)
+{
+	struct flow_rq_data *rd = get_rq_data(rq);
+
+	lockdep_assert_held(&fd->lock);
+	/* NULL priv[0] guard — prepare_request may have failed. */
+	if (rd) {
+		if (!list_empty(&rd->dl_node))
+			flow_del_from_dl_tree(fd, rd->lane, rq);
+	}
+
+	flow_del_from_merge_rb(fd, rq);
+
+	list_del_init(&rq->queuelist);
+	elv_rqhash_del(q, rq);
+	if (q->last_merge == rq)
+		q->last_merge = NULL;
+}
+
+/* ── Insert request ───────────────────────────────────────────────── */
+
+
+/**
+ * flow_insert_request - Insert a request into the scheduler
+ * @hctx: hardware context
+ * @rq: request to insert
+ * @insert_flags: BLK_MQ_INSERT_AT_HEAD etc.
+ * @free: list for merged-and-consumed requests to be reclaimed after locks released
+ *
+ * Return: true on success, false on allocation failure (caller must fail the request)
+ */
+static bool flow_insert_request(struct blk_mq_hw_ctx *hctx,
+				struct request *rq, blk_insert_t insert_flags,
+				struct list_head *free)
+{
+	struct request_queue *q = hctx->queue;
+	struct flow_data *fd = q->elevator->elevator_data;
+	struct flow_rq_data *rd = get_rq_data(rq);
+	struct io_cq *icq = rq->elv.icq;	/* use rq->elv.icq for io_cq access */
+	struct flow_icq_data *ficq = NULL;
+	u8 lane;
+	bool contained = false;
+
+	lockdep_assert_held(&fd->lock);
+	/* guard against NULL priv[0] when mempool_alloc fails in prepare_request */
+	if (!rd)
+		return true;					/* nothing to schedule — block layer owns the completion */
+
+	rd->block_size = blk_rq_bytes(rq);
+
+	/* Attempt merge with existing requests in scheduler */
+	if (blk_mq_sched_try_insert_merge(q, rq, free))
+		return true;
+
+	/* Budget / containment check */
+	if (icq) {
+		ficq = icq_to_flow_icq(icq);
+		if (ficq) {
+			contained = flow_check_budget_and_contain(ficq, rq, fd, hctx);
+			/* Recompute IO profile from recent budget/signal state */
+			if (ktime_get_ns() - atomic64_read(&ficq->last_io_profile_update) >
+			    NSEC_PER_MSEC)
+				flow_recompute_io_profile(ficq, rq, fd);
+		}
+	}
+
+	lane = flow_assign_lane(rq, insert_flags, fd);
+
+	/* Override to contained lane if budget exhausted */
+	if (contained && lane < FLOW_LANE_CONTAINED)
+		lane = FLOW_LANE_CONTAINED;
+
+	WRITE_ONCE(rd->lane, lane);
+
+	/* Tier 0: Emergency → prio_queue */
+	if (lane == FLOW_LANE_EMERGENCY) {
+		list_add_tail(&rq->queuelist, &fd->prio_queue[0]);
+		goto done_insert;
+	}
+
+	/* Tiers 1-4: deadline-sorted rbtree */
+	if (!flow_add_to_dl_tree(fd, lane, rq))
+		return false;					/* allocation failure — caller must fail the I/O */
+
+done_insert:
+	/* Make mergeable requests visible to bio merge */
+	if (rq_mergeable(rq)) {
+		flow_add_to_merge_rb(fd, rq);
+		elv_rqhash_add(q, rq);
+		if (!q->last_merge)
+			q->last_merge = rq;
+	}
+	return true;
+}
+
+static void flow_insert_requests(struct blk_mq_hw_ctx *hctx,
+				 struct list_head *list,
+				 blk_insert_t insert_flags)
+{
+	struct request_queue *q = hctx->queue;
+	struct flow_data *fd = q->elevator->elevator_data;
+	struct request *rq, *tmp;
+	bool stop = false;
+	LIST_HEAD(free);
+	LIST_HEAD(fail);
+
+	do {
+		scoped_guard(spinlock_irqsave, &fd->lock)
+		for (int i = 0; i < FLOW_MAX_INSERTS_PER_LOCK; i++) {
+			if (list_empty(list)) {
+				stop = true;
+				break;
+			}
+			rq = list_first_entry(list, struct request, queuelist);
+			list_del_init(&rq->queuelist);
+			if (!flow_insert_request(hctx, rq, insert_flags, &free)) {
+				/* Allocation failure — failed to schedule */
+				list_add_tail(&rq->queuelist, &fail);
+			}
+		}
+	} while (!stop);
+
+	blk_mq_free_requests(&free);
+
+	/* Complete failed requests with I/O error (after releasing fd->lock) */
+	list_for_each_entry_safe(rq, tmp, &fail, queuelist) {
+		list_del_init(&rq->queuelist);
+		blk_mq_end_request(rq, BLK_STS_IOERR);
+	}
+}
+
+/* ── Prepare / finish request lifecycle ───────────────────────────── */
+
+static void flow_prepare_request(struct request *rq)
+{
+	struct flow_data *fd = rq->q->elevator->elevator_data;
+	struct flow_rq_data *rd;
+
+	rd = mempool_alloc(fd->rq_data_pool, GFP_ATOMIC);
+	if (!rd)
+		return;
+	memset(rd, 0, sizeof(*rd));
+	rd->rq = rq;
+	INIT_LIST_HEAD(&rd->dl_node);
+	rq->elv.priv[0] = rd;
+}
+
+static void flow_finish_request(struct request *rq)
+{
+	struct flow_data *fd = rq->q->elevator->elevator_data;
+
+	if (rq->elv.priv[0]) {
+		mempool_free(get_rq_data(rq), fd->rq_data_pool);
+		rq->elv.priv[0] = NULL;
+	}
+}
+
+/* ── io_cq lifecycle ──────────────────────────────────────────── */
+
+static void flow_init_icq(struct io_cq *icq)
+{
+	struct flow_icq_data *ficq = icq_to_flow_icq(icq);
+
+	if (!ficq)
+		return;
+
+	memset(ficq, 0, sizeof(*ficq));
+	atomic64_set(&ficq->io_budget_sectors,
+		     FLOW_BUDGET_SECTORS_DEFAULT_SYNC);
+	atomic_set(&ficq->containment_score, 0);
+	atomic_set(&ficq->latency_allowance, 0);
+	atomic_set(&ficq->latency_pressure, 0);
+	atomic_set(&ficq->io_profile, 0);
+	atomic64_set(&ficq->last_io_completed, ktime_get_ns());
+	atomic64_set(&ficq->last_io_inserted, ktime_get_ns());
+	atomic64_set(&ficq->last_io_profile_update, ktime_get_ns());
+}
+
+static void flow_exit_icq(struct io_cq *icq)
+{
+	struct flow_icq_data *ficq = icq_to_flow_icq(icq);
+
+	if (!ficq)
+		return;
+
+	/* Reset all fields atomically — concurrent completion callbacks
+	 * on another CPU may still read these values briefly.
+	 */
+	atomic64_set(&ficq->io_budget_sectors, 0);
+	atomic_set(&ficq->containment_score, 0);
+	atomic_set(&ficq->latency_allowance, 0);
+	atomic_set(&ficq->latency_pressure, 0);
+	atomic_set(&ficq->io_profile, 0);
+	atomic64_set(&ficq->last_io_completed, 0);
+	atomic64_set(&ficq->last_io_inserted, 0);
+	atomic64_set(&ficq->last_io_profile_update, 0);
+}
+
+/* ── Per-hctx dispatch ──────────────────────────────────────────── */
+
+static inline u8 flow_starved_lane(struct flow_data *fd,
+				   struct flow_hctx_data *khd)
+{
+	lockdep_assert_held(&fd->lock);
+	/*
+	 * Check lanes that have exceeded their starvation threshold.
+	 * Uses tuned_starvation_max which may be adjusted by the autotuner.
+	 */
+	for (u8 lane = FLOW_LANE_CONTAINED; lane > FLOW_LANE_EMERGENCY; lane--) {
+		u32 max = READ_ONCE(fd->tuned_starvation_max[lane]) ?
+			  READ_ONCE(fd->tuned_starvation_max[lane]) :
+			  fd->starvation_max[lane];
+		if (max &&
+		    khd->starvation_rounds[lane] >= max &&
+		    flow_first_rq_for_lane(fd, lane))
+			return lane;
+	}
+	return FLOW_LANE_EMERGENCY;
+}
+
+/*
+ * Fill a per-hctx dispatch list from the global deadline rbtrees.
+ * Called under fd->lock. Appends to khd->dispatch_list under khd->lock.
+ * Lock ordering: fd->lock → khd->lock.
+ *
+ * Dispatch order with quota mechanism (adapted from scx_flow):
+ *   1. Starvation override (shared/contained if rounds >= max)
+ *   2. Quota override (if high_priority bursts >= quota_max, force lower lanes)
+ *   3. Normal priority: Reserved → Latency → Shared → Contained
+ *   4. High-priority lanes (<= Latency) increment quota, low-priority (>= Shared) reset it
+ */
+static bool flow_fill_dispatch_locked(struct flow_data *fd,
+				      struct flow_hctx_data *khd)
+{
+	lockdep_assert_held(&fd->lock);
+	u32 rd_count = 0, wr_count = 0;
+	bool filled = false;
+	u32 quota_max = fd->high_priority_quota_max;
+
+	for (int i = 0; i < FLOW_MAX_DELETES_PER_LOCK; i++) {
+		struct flow_rq_data *rd;
+		struct request *rq;
+		u8 lane;
+
+		/* Step 1: Check starvation before normal dispatch order */
+		lane = flow_starved_lane(fd, khd);
+		if (lane == FLOW_LANE_EMERGENCY) {
+			/* Step 2: Quota override — force lower lanes when high-priority
+			 * bursts have exceeded the quota max. This matches scx_flow's
+			 * quota_force_shared / quota_force_contained mechanism.
+			 */
+			if (khd->high_priority_burst_rounds >= quota_max) {
+				rd = flow_first_rq_for_lane(fd, FLOW_LANE_SHARED);
+				if (rd) {
+					lane = FLOW_LANE_SHARED;
+					khd->high_priority_burst_rounds = 0;
+					goto dispatch_rd;
+				}
+				rd = flow_first_rq_for_lane(fd, FLOW_LANE_CONTAINED);
+				if (rd) {
+					lane = FLOW_LANE_CONTAINED;
+					khd->high_priority_burst_rounds = 0;
+					goto dispatch_rd;
+				}
+			}
+
+			/* Step 3: Normal dispatch order: Reserved → Latency → Shared → Contained */
+			lane = FLOW_LANE_RESERVED;
+			rd = flow_first_rq_for_lane(fd, lane);
+			if (!rd) { lane = FLOW_LANE_LATENCY;
+				rd = flow_first_rq_for_lane(fd, lane); }
+			if (!rd) { lane = FLOW_LANE_SHARED;
+				rd = flow_first_rq_for_lane(fd, lane); }
+			if (!rd) { lane = FLOW_LANE_CONTAINED;
+				rd = flow_first_rq_for_lane(fd, lane); }
+		} else {
+			/* Starvation override: pick from the starved lane */
+			rd = flow_first_rq_for_lane(fd, lane);
+			khd->starvation_rounds[lane] = 0;
+		}
+		if (!rd)
+			break;
+
+dispatch_rd:
+		/* Increment starvation for all lanes below this one */
+		for (u8 l = lane + 1; l < FLOW_NR_LANES; l++) {
+			if (khd->starvation_rounds[l] < U32_MAX)
+				khd->starvation_rounds[l]++;
+		}
+
+		/* Step 4: Update quota burst counter
+		 * High-priority (Reserved/Latency) increments the quota counter.
+		 * Low-priority (Shared/Contained) resets it.
+		 */
+		if (lane <= FLOW_LANE_LATENCY && lane >= FLOW_LANE_RESERVED) {
+			if (khd->high_priority_burst_rounds < U32_MAX)
+				khd->high_priority_burst_rounds++;
+		} else if (lane >= FLOW_LANE_SHARED) {
+			khd->high_priority_burst_rounds = 0;
+		}
+
+		/* Update per-hctx metrics counters */
+		switch (lane) {
+		case FLOW_LANE_RESERVED:
+			atomic64_inc(&khd->m_reserved_dispatch);
+			break;
+		case FLOW_LANE_LATENCY:
+			atomic64_inc(&khd->m_latency_dispatch);
+			break;
+		case FLOW_LANE_SHARED:
+			atomic64_inc(&khd->m_shared_dispatch);
+			if (flow_first_rq_for_lane(fd, FLOW_LANE_RESERVED) ||
+			    flow_first_rq_for_lane(fd, FLOW_LANE_LATENCY))
+				atomic64_inc(&khd->m_shared_rescue);
+			break;
+		case FLOW_LANE_CONTAINED:
+			atomic64_inc(&khd->m_contained_dispatch);
+			if (flow_first_rq_for_lane(fd, FLOW_LANE_RESERVED) ||
+			    flow_first_rq_for_lane(fd, FLOW_LANE_LATENCY))
+				atomic64_inc(&khd->m_contained_rescue);
+			break;
+		}
+
+		rq = rd->rq;
+		u8 optype = flow_optype(rq);
+
+		/* Enforce per-hctx batch limits — remove and reinsert to advance cursor */
+		if (optype == FLOW_READ && rd_count >= fd->batch_max_read) {
+			flow_remove_request(fd, rq, rq->q);
+			if (!flow_add_to_dl_tree(fd, lane, rq)) {
+				spin_lock(&khd->lock);
+				list_add_tail(&rq->queuelist, &khd->dispatch_list);
+				spin_unlock(&khd->lock);
+				if (optype == FLOW_READ) rd_count++;
+			}
+			continue;
+		}
+		if (optype == FLOW_WRITE && wr_count >= fd->batch_max_write) {
+			flow_remove_request(fd, rq, rq->q);
+			if (!flow_add_to_dl_tree(fd, lane, rq)) {
+				spin_lock(&khd->lock);
+				list_add_tail(&rq->queuelist, &khd->dispatch_list);
+				spin_unlock(&khd->lock);
+				if (optype == FLOW_WRITE) wr_count++;
+			}
+			continue;
+		}
+
+		flow_remove_request(fd, rq, rq->q);
+
+		/* Append to per-hctx dispatch list under khd->lock */
+		spin_lock(&khd->lock);
+		list_add_tail(&rq->queuelist, &khd->dispatch_list);
+		spin_unlock(&khd->lock);
+
+		if (optype == FLOW_READ)
+			rd_count++;
+		else if (optype == FLOW_WRITE)
+			wr_count++;
+		filled = true;
+	}
+
+	return filled;
+}
+
+/* ── Dispatch ─────────────────────────────────────────────────────── */
+
+static struct request *flow_dispatch_from_prio(struct flow_data *fd)
+{
+	struct request *rq;
+
+	lockdep_assert_held(&fd->lock);
+	/* prio_queue[0] is highest priority (Emergency) */
+	if (!list_empty(&fd->prio_queue[0])) {
+		rq = list_first_entry(&fd->prio_queue[0], struct request,
+				     queuelist);
+		list_del_init(&rq->queuelist);
+		return rq;
+	}
+
+	/* prio_queue[1] holds barrier / flush requests */
+	if (!list_empty(&fd->prio_queue[1])) {
+		rq = list_first_entry(&fd->prio_queue[1], struct request,
+				     queuelist);
+		list_del_init(&rq->queuelist);
+		return rq;
+	}
+
+	return NULL;
+}
+
+static struct request *flow_dispatch_request(struct blk_mq_hw_ctx *hctx)
+{
+	struct flow_data *fd = hctx->queue->elevator->elevator_data;
+	struct flow_hctx_data *khd = hctx->sched_data;
+	struct request *rq;
+	unsigned long flags;
+
+	/*
+	 * Phase 1: Fast path — pop from per-hctx dispatch list.
+	 * Only acquires per-hctx lock, no global contention.
+	 */
+	spin_lock_irqsave(&khd->lock, flags);
+	rq = list_first_entry_or_null(&khd->dispatch_list, struct request,
+				      queuelist);
+	if (rq) {
+		list_del_init(&rq->queuelist);
+		spin_unlock_irqrestore(&khd->lock, flags);
+		return rq;
+	}
+	spin_unlock_irqrestore(&khd->lock, flags);
+
+	/*
+	 * Phase 2: Refill from global rbtrees.
+	 * khd->lock is NOT held here — lock ordering: fd->lock → khd->lock.
+	 */
+	guard(spinlock_irqsave)(&fd->lock);
+
+	/* 2a. Emergency / barrier prio queues — global, checked under fd->lock */
+	rq = flow_dispatch_from_prio(fd);
+	if (rq)
+		return rq;
+
+	/* 2b. Fill this hctx's dispatch list from global rbtrees */
+	flow_fill_dispatch_locked(fd, khd);
+
+	/* 2c. Pop from now-filled dispatch list (briefly take khd->lock) */
+	spin_lock_irqsave(&khd->lock, flags);
+	rq = list_first_entry_or_null(&khd->dispatch_list, struct request,
+				      queuelist);
+	if (rq)
+		list_del_init(&rq->queuelist);
+	spin_unlock_irqrestore(&khd->lock, flags);
+
+	return rq;
+}
+
+/* ── Completion ───────────────────────────────────────────────────── */
+
+static void flow_completed_request(struct request *rq, u64 now)
+{
+	struct io_cq *icq = rq->elv.icq;
+	struct flow_icq_data *ficq;
+
+	if (!icq)
+		return;
+
+	ficq = icq_to_flow_icq(icq);
+	if (!ficq)
+		return;
+
+	/*
+	 * Lock-free atomic access — this runs from softirq completion context
+	 * and may race with flow_insert_request on another CPU. All per-ICQ
+	 * fields are atomic_t or atomic64_t.
+	 */
+	atomic64_set(&ficq->last_io_completed, now);
+
+	/*
+	 * Primary budget refill: return (completed_sectors / FLOW_REFILL_DIV)
+	 * to the process budget. This mirrors scx_flow's proportional refill
+	 * on every event (wakeup in CPU scheduling, completion in I/O).
+	 */
+	s64 sectors = (s64)blk_rq_sectors(rq);
+	s64 refill = sectors / FLOW_REFILL_DIV;
+	if (refill > 0) {
+		struct flow_data *fd = rq->q->elevator->elevator_data;
+		s64 budget = (rq->cmd_flags & REQ_SYNC) ?
+			     fd->sync_budget_sectors : fd->async_budget_sectors;
+		if (budget < 64) budget = 64;
+		s64 new_budget = atomic64_add_return(refill, &ficq->io_budget_sectors);
+		s64 cap = budget * 4;
+		if (unlikely(new_budget > cap))
+			atomic64_set(&ficq->io_budget_sectors, cap);
+
+		/* Raise latency allowance when budget grows after successful completion.
+		 * This mirrors scx_flow's raise_latency_allowance triggered by
+		 * positive budget refill after sleep.
+		 */
+		if (new_budget >= budget)
+			flow_raise_bounded_atomic(&ficq->latency_allowance, 1,
+						  FLOW_LATENCY_CREDIT_MAX);
+	}
+
+	/* Decay containment score (geometric) */
+	flow_decay_geometric_atomic(&ficq->containment_score,
+				    FLOW_HOG_SCORE_DECAY_SHIFT);
+
+	/* Decay latency pressure (geometric) */
+	flow_decay_geometric_atomic(&ficq->latency_pressure,
+				    FLOW_LATENCY_DEBT_DECAY_SHIFT);
+
+	/* Raise latency pressure if containment is active — the process is being
+	 * starved, so track its latency debt like scx_flow's raise_latency_pressure.
+	 */
+	if (atomic_read(&ficq->containment_score) >= FLOW_CONTAIN_THRESHOLD_DEFAULT)
+		flow_raise_bounded_atomic(&ficq->latency_pressure, 1,
+					  FLOW_LATENCY_DEBT_MAX);
+
+	/* Recompute IO profile periodically */
+	if (now - atomic64_read(&ficq->last_io_profile_update) > NSEC_PER_MSEC)
+		flow_recompute_io_profile(ficq, rq, rq->q->elevator->elevator_data);
+}
+
+/* ── Limit depth (async throttle) ─────────────────────────────────── */
+
+static int flow_to_word_depth(struct blk_mq_hw_ctx *hctx, unsigned int qdepth)
+{
+	struct sbitmap_queue *bt = &hctx->sched_tags->bitmap_tags;
+	const unsigned int nrr = hctx->queue->nr_requests;
+
+	return ((qdepth << bt->sb.shift) + nrr - 1) / nrr;
+}
+
+static void flow_limit_depth(blk_opf_t opf, struct blk_mq_alloc_data *data)
+{
+	struct flow_data *fd = data->q->elevator->elevator_data;
+
+	/* Don't throttle synchronous reads */
+	if (op_is_sync(opf) && !op_is_write(opf))
+		return;
+
+	data->shallow_depth = flow_to_word_depth(data->hctx,
+				data->q->nr_requests / FLOW_ASYNC_DEPTH_RATIO);
+	(void)fd;	/* unused in this version; kept for future parameter use */
+}
+
+/* ── Allow merge ──────────────────────────────────────────────────── */
+
+static bool flow_allow_merge(struct request_queue *q, struct request *rq,
+			     struct bio *bio)
+{
+	struct flow_rq_data *rd = get_rq_data(rq);
+
+	/* Basic direction check */
+	if (bio_data_dir(bio) != rq_data_dir(rq))
+		return false;
+
+	/* reject merges that cross lane boundaries (avoids priority inversion) */
+	if (rd) {
+		u8 rq_lane = READ_ONCE(rd->lane);
+		u8 bio_lane;
+
+		/* Determine what lane the bio would land in */
+		if (bio->bi_opf & REQ_META || bio->bi_opf & REQ_PRIO)
+			bio_lane = FLOW_LANE_RESERVED;
+		else if ((bio->bi_opf & REQ_SYNC) || bio->bi_iter.bi_size <= 4096)
+			bio_lane = FLOW_LANE_LATENCY;
+		else
+			bio_lane = FLOW_LANE_SHARED;
+
+		if (rq_lane != bio_lane)
+			return false;
+	}
+
+	return true;
+}
+
+/* ── Has work ─────────────────────────────────────────────────────── */
+
+static bool flow_has_work(struct blk_mq_hw_ctx *hctx)
+{
+	struct flow_data *fd = hctx->queue->elevator->elevator_data;
+	struct flow_hctx_data *khd = hctx->sched_data;
+
+	/* Fast check: per-hctx dispatch list (lock-free via list_empty_careful) */
+	if (!list_empty_careful(&khd->dispatch_list))
+		return true;
+
+	/* Slow check: global rbtrees under fd->lock */
+	guard(spinlock_irqsave)(&fd->lock);
+	return !list_empty(&fd->prio_queue[0]) ||
+	       !list_empty(&fd->prio_queue[1]) ||
+	       !RB_EMPTY_ROOT(&fd->reserved_root.rb_root) ||
+	       !RB_EMPTY_ROOT(&fd->latency_root.rb_root) ||
+	       !RB_EMPTY_ROOT(&fd->shared_root.rb_root) ||
+	       !RB_EMPTY_ROOT(&fd->contained_root.rb_root);
+}
+
+/* ── Autotune timer ────────────────────────────────────────────────── */
+
+/*
+ * 3-mode autotuner adapted from scx_flow's AutoTuner::evaluate_mode().
+ * Runs every second, aggregates per-hctx metrics, computes workload ratios,
+ * selects a mode (Balanced/Latency/Throughput), and steps tunables toward
+ * mode targets using scx_flow's step_towards() pattern.
+ */
+static void flow_autotune_timer_fn(struct timer_list *t)
+{
+	struct flow_data *fd = container_of(t, struct flow_data, autotune_timer);
+	struct blk_mq_hw_ctx *hctx;
+	unsigned long i;
+	u64 total_reserved = 0, total_latency = 0, total_shared = 0;
+	u64 total_contained = 0, total_rescue = 0;
+	u64 total_dispatch;
+	u32 rescue_ratio, latency_ratio, contained_ratio, shared_ratio;
+	u32 new_mode;
+	bool enter_latency, enter_throughput, rebalance;
+
+	queue_for_each_hw_ctx(fd->queue, hctx, i) {
+		struct flow_hctx_data *khd = hctx->sched_data;
+
+		if (!khd)
+			continue;
+		guard(spinlock_irqsave)(&khd->lock);
+		/*
+		 * Metrics counters are atomic64_t — race-free between
+		 * dispatch (atomic64_inc under fd->lock) and the aggregation
+		 * here (atomic64_read/atomic64_set under khd->lock).
+		 */
+		total_reserved	+= atomic64_read(&khd->m_reserved_dispatch);
+		total_latency	+= atomic64_read(&khd->m_latency_dispatch);
+		total_shared	+= atomic64_read(&khd->m_shared_dispatch);
+		total_contained	+= atomic64_read(&khd->m_contained_dispatch);
+		total_rescue	+= atomic64_read(&khd->m_contained_rescue) +
+				   atomic64_read(&khd->m_shared_rescue);
+		atomic64_set(&khd->m_reserved_dispatch, 0);
+		atomic64_set(&khd->m_latency_dispatch, 0);
+		atomic64_set(&khd->m_shared_dispatch, 0);
+		atomic64_set(&khd->m_contained_dispatch, 0);
+		atomic64_set(&khd->m_contained_rescue, 0);
+		atomic64_set(&khd->m_shared_rescue, 0);
+		atomic64_set(&khd->m_budget_exhaustions, 0);
+		atomic64_set(&khd->m_latency_debt_raises, 0);
+		atomic64_set(&khd->m_hog_containments, 0);
+	}
+
+	total_dispatch = total_reserved + total_latency +
+			 total_shared + total_contained;
+	if (total_dispatch < 4)
+		goto reschedule;
+
+	rescue_ratio = (total_rescue * 100) /
+		       max(total_contained + total_shared, 1ULL);
+	latency_ratio = (total_latency * 100) / max(total_dispatch, 1ULL);
+	contained_ratio = (total_contained * 100) / max(total_dispatch, 1ULL);
+	shared_ratio = (total_shared * 100) / max(total_dispatch, 1ULL);
+	new_mode = fd->autotune_mode;
+
+	enter_latency = latency_ratio > 40 || rescue_ratio > 15;
+	enter_throughput = contained_ratio > 12 && shared_ratio < 45;
+	rebalance = rescue_ratio > 8 && latency_ratio < 40 &&
+		    shared_ratio < 45;
+
+	if (rebalance)
+		new_mode = FLOW_AUTOTUNE_BALANCED;
+	else if (enter_latency)
+		new_mode = FLOW_AUTOTUNE_LATENCY;
+	else if (enter_throughput)
+		new_mode = FLOW_AUTOTUNE_THROUGHPUT;
+
+	/*
+	 * Apply tuned parameters. The autotune modifies batch_max_read/write
+	 * and tuned_starvation_max[] directly. The I/O path reads batch_max_read
+	 * under fd->lock (safe) and tuned_starvation_max[] via READ_ONCE.
+	 */
+	if (new_mode == FLOW_AUTOTUNE_LATENCY) {
+		u32 v = READ_ONCE(fd->tuned_starvation_max[FLOW_LANE_CONTAINED]);
+		if (v > 8)
+			WRITE_ONCE(fd->tuned_starvation_max[FLOW_LANE_CONTAINED], v - 1);
+		else if (v < 8)
+			WRITE_ONCE(fd->tuned_starvation_max[FLOW_LANE_CONTAINED], v + 1);
+		guard(spinlock_irqsave)(&fd->lock);
+		if (fd->batch_max_read > 8)
+			fd->batch_max_read--;
+	} else if (new_mode == FLOW_AUTOTUNE_THROUGHPUT) {
+		u32 v = READ_ONCE(fd->tuned_starvation_max[FLOW_LANE_CONTAINED]);
+		if (v < 20)
+			WRITE_ONCE(fd->tuned_starvation_max[FLOW_LANE_CONTAINED], v + 1);
+		guard(spinlock_irqsave)(&fd->lock);
+		if (fd->batch_max_read < 32)
+			fd->batch_max_read++;
+	} else {
+		u32 v = READ_ONCE(fd->tuned_starvation_max[FLOW_LANE_CONTAINED]);
+		if (v < 15)
+			WRITE_ONCE(fd->tuned_starvation_max[FLOW_LANE_CONTAINED], v + 1);
+		else if (v > 15)
+			WRITE_ONCE(fd->tuned_starvation_max[FLOW_LANE_CONTAINED], v - 1);
+		guard(spinlock_irqsave)(&fd->lock);
+		if (fd->batch_max_read < 16)
+			fd->batch_max_read++;
+		else if (fd->batch_max_read > 16)
+			fd->batch_max_read--;
+	}
+
+reschedule:
+	mod_timer(&fd->autotune_timer, jiffies + HZ * FLOW_AUTOTUNE_INTERVAL_HZ);
+}
+
+/* ── Init / Exit ──────────────────────────────────────────────────── */
+
+/* 6.18+ / 7.x init_sched signature: passes a pre-allocated elevator_queue.
+ * For kernels 6.12-6.17 (which pass elevator_type), apply
+ * patches/0002-linux6.12-flow-iosched-compat.patch after this one.     */
+static int flow_init_sched(struct request_queue *q, struct elevator_queue *eq)
+{
+	struct flow_data *fd;
+	int ret = -ENOMEM;
+
+	fd = kzalloc_node(sizeof(*fd), GFP_KERNEL, q->node);
+	if (!fd) {
+		pr_err("flow-iosched: failed to allocate flow_data\n");
+		return ret;
+	}
+
+	/* Create memory pools */
+	fd->rq_data_cache = kmem_cache_create("flow_rq_data",
+					      sizeof(struct flow_rq_data),
+					      0, SLAB_HWCACHE_ALIGN, NULL);
+	if (!fd->rq_data_cache) {
+		pr_err("flow-iosched: failed to create rq_data cache\n");
+		goto free_fd;
+	}
+
+	fd->rq_data_pool = mempool_create_slab_pool(q->nr_requests,
+						    fd->rq_data_cache);
+	if (!fd->rq_data_pool) {
+		pr_err("flow-iosched: failed to create rq_data pool\n");
+		goto destroy_rq_cache;
+	}
+
+	fd->dl_group_cache = kmem_cache_create("flow_dl_group",
+					       sizeof(struct dl_group),
+					       0, SLAB_HWCACHE_ALIGN, NULL);
+	if (!fd->dl_group_cache) {
+		pr_err("flow-iosched: failed to create dl_group cache\n");
+		goto destroy_rq_pool;
+	}
+
+	/* Initialise queues */
+	for (int i = 0; i < 2; i++)
+		INIT_LIST_HEAD(&fd->prio_queue[i]);
+
+	/* Initialise deadline trees */
+	fd->reserved_root	= RB_ROOT_CACHED;
+	fd->latency_root	= RB_ROOT_CACHED;
+	fd->shared_root		= RB_ROOT_CACHED;
+	fd->contained_root	= RB_ROOT_CACHED;
+	fd->merge_root		= RB_ROOT_CACHED;
+
+	/* Budget defaults */
+	fd->sync_budget_sectors		= FLOW_BUDGET_SECTORS_DEFAULT_SYNC;
+	fd->async_budget_sectors	= FLOW_BUDGET_SECTORS_DEFAULT_ASYNC;
+
+	/* Containment defaults */
+	fd->contain_threshold		= FLOW_CONTAIN_THRESHOLD_DEFAULT;
+	fd->contain_decay_step		= FLOW_CONTAIN_DECAY_STEP_DEFAULT;
+
+	/* Starvation thresholds (applied per-hctx via starvation_rounds) */
+	fd->starvation_max[FLOW_LANE_RESERVED]	= FLOW_STARVATION_MAX_DEFAULT_RESERVED;
+	fd->starvation_max[FLOW_LANE_LATENCY]	= FLOW_STARVATION_MAX_DEFAULT_RESERVED;
+	fd->starvation_max[FLOW_LANE_SHARED]	= FLOW_STARVATION_MAX_DEFAULT_SHARED;
+	fd->starvation_max[FLOW_LANE_CONTAINED]	= FLOW_STARVATION_MAX_DEFAULT_CONTAINED;
+
+	/* Batch defaults */
+	fd->batch_max_read		= FLOW_BATCH_MAX_READ_DEFAULT;
+	fd->batch_max_write		= FLOW_BATCH_MAX_WRITE_DEFAULT;
+	fd->completion_window_ns	= FLOW_COMPLETION_WINDOW_NS_DEFAULT;
+
+	/* Read priority bias */
+	fd->read_priority = 0;
+
+	/* Rotational detection */
+	fd->is_rotational = !!(q->limits.features & BLK_FEAT_ROTATIONAL);
+
+	spin_lock_init(&fd->lock);
+
+	/* Autotune state */
+	fd->autotune_mode = FLOW_AUTOTUNE_BALANCED;
+	fd->autotune_generation = 0;
+	timer_setup(&fd->autotune_timer, flow_autotune_timer_fn, 0);
+
+	/* Tuned parameters (start at sysfs defaults, autotune adjusts) */
+	for (int i = 0; i < FLOW_NR_LANES; i++)
+		fd->tuned_starvation_max[i] = fd->starvation_max[i];
+	fd->high_priority_quota_max = FLOW_HIGH_PRIORITY_QUOTA_MAX_DEFAULT;
+	fd->reserved_lane_burst_max = FLOW_RESERVED_LANE_BURST_MAX_DEFAULT;
+
+	eq->elevator_data = fd;
+	q->elevator = eq;
+
+	/* Multi-queue dispatch — each hctx dispatches independently */
+	blk_queue_flag_clear(QUEUE_FLAG_SQ_SCHED, q);
+
+	fd->queue = q;
+	blk_stat_enable_accounting(q);
+
+	/* Start autotune timer (fires after 1 second) */
+	mod_timer(&fd->autotune_timer, jiffies + HZ * FLOW_AUTOTUNE_INTERVAL_HZ);
+
+	return 0;
+
+destroy_rq_pool:
+	mempool_destroy(fd->rq_data_pool);
+destroy_rq_cache:
+	kmem_cache_destroy(fd->rq_data_cache);
+free_fd:
+	kfree(fd);
+	return ret;
+}
+
+static void flow_exit_sched(struct elevator_queue *e)
+{
+	struct flow_data *fd = e->elevator_data;
+
+	/* Delete autotune timer before draining scheduler state */
+	del_timer_sync(&fd->autotune_timer);
+
+	WARN_ON_ONCE(!list_empty(&fd->prio_queue[0]));
+	WARN_ON_ONCE(!list_empty(&fd->prio_queue[1]));
+
+	/* Drain any pending requests still in the scheduler */
+	{
+		struct request *rq, *n;
+		LIST_HEAD(drain);
+
+		/* Collect from deadline rbtrees (simplified — all lanes) */
+		for (u8 lane = FLOW_LANE_RESERVED; lane < FLOW_NR_LANES; lane++) {
+			struct rb_root_cached *root = flow_root_for_lane(fd, lane);
+			struct rb_node *node;
+
+			if (!root)
+				continue;
+			while ((node = rb_first_cached(root))) {
+				struct dl_group *dlg = rb_entry(node, struct dl_group, node);
+				struct flow_rq_data *rd;
+
+				while ((rd = list_first_entry_or_null(&dlg->rqs,
+					struct flow_rq_data, dl_node))) {
+					list_del_init(&rd->dl_node);
+					rq = rd->rq;
+					list_add_tail(&rq->queuelist, &drain);
+				}
+				rb_erase_cached(&dlg->node, root);
+				kmem_cache_free(fd->dl_group_cache, dlg);
+			}
+		}
+
+		/* Move drain list entries to the block layer for completion */
+		list_for_each_entry_safe(rq, n, &drain, queuelist) {
+			list_del_init(&rq->queuelist);
+			if (rq->elv.priv[0]) {
+				mempool_free(get_rq_data(rq), fd->rq_data_pool);
+				rq->elv.priv[0] = NULL;
+			}
+		}
+	}
+
+	mempool_destroy(fd->rq_data_pool);
+	kmem_cache_destroy(fd->rq_data_cache);
+	kmem_cache_destroy(fd->dl_group_cache);
+
+	blk_stat_disable_accounting(fd->queue);
+
+	kfree(fd);
+}
+
+/* ── Per-hctx init / exit ─────────────────────────────────────────── */
+
+static int flow_init_hctx(struct blk_mq_hw_ctx *hctx, unsigned int hctx_idx)
+{
+	struct flow_hctx_data *khd;
+
+	khd = kzalloc_node(sizeof(*khd), GFP_KERNEL, hctx->numa_node);
+	if (!khd) {
+		pr_err("flow-iosched: init_hctx hctx-%u (node %d) failed\n",
+		       hctx_idx, hctx->numa_node);
+		return -ENOMEM;
+	}
+
+	spin_lock_init(&khd->lock);
+	INIT_LIST_HEAD(&khd->dispatch_list);
+	memset(khd->starvation_rounds, 0, sizeof(khd->starvation_rounds));
+	khd->high_priority_burst_rounds = 0;
+	khd->hctx = hctx;
+	/* All atomic64_t metrics counters are already 0 from kzalloc. */
+
+	hctx->sched_data = khd;
+	return 0;
+}
+
+static void flow_exit_hctx(struct blk_mq_hw_ctx *hctx, unsigned int hctx_idx)
+{
+	struct flow_hctx_data *khd = hctx->sched_data;
+
+	if (!khd)
+		return;
+
+	if (WARN_ON_ONCE(!list_empty(&khd->dispatch_list))) {
+		struct request *rq, *n;
+
+		list_for_each_entry_safe(rq, n, &khd->dispatch_list, queuelist)
+			list_del_init(&rq->queuelist);
+	}
+
+	kfree(khd);
+	hctx->sched_data = NULL;
+}
+
+/* ── Sysfs attributes ─────────────────────────────────────────────── */
+
+static ssize_t flow_version_show(struct elevator_queue *e, char *page)
+{
+	return sprintf(page, "%s\n", FLOW_VERSION);
+}
+
+static ssize_t flow_read_priority_show(struct elevator_queue *e, char *page)
+{
+	struct flow_data *fd = e->elevator_data;
+	guard(spinlock_irqsave)(&fd->lock);
+	return sprintf(page, "%d\n", fd->read_priority);
+}
+
+static ssize_t flow_read_priority_store(struct elevator_queue *e,
+					const char *page, size_t count)
+{
+	struct flow_data *fd = e->elevator_data;
+	int prio;
+	int ret;
+
+	ret = kstrtoint(page, 10, &prio);
+	if (ret || prio < -20 || prio > 19)
+		return -EINVAL;
+
+	guard(spinlock_irqsave)(&fd->lock);
+	fd->read_priority = prio;
+	return count;
+}
+
+/* all sysfs store/show paths must hold the scheduler lock */
+#define FLOW_SYSFS_S32_RW(field)						\
+static ssize_t flow_##field##_show(struct elevator_queue *e, char *page)	\
+{										\
+	struct flow_data *fd = e->elevator_data;				\
+	guard(spinlock_irqsave)(&fd->lock);					\
+	return sprintf(page, "%d\n", fd->field);				\
+}										\
+static ssize_t flow_##field##_store(struct elevator_queue *e,			\
+				    const char *page, size_t count)		\
+{										\
+	struct flow_data *fd = e->elevator_data;				\
+	int val;								\
+	int ret;								\
+	ret = kstrtoint(page, 10, &val);					\
+	if (ret || val < 0)							\
+		return -EINVAL;							\
+	guard(spinlock_irqsave)(&fd->lock);					\
+	fd->field = val;							\
+	return count;								\
+}
+
+#define FLOW_SYSFS_U32_RW(field)						\
+static ssize_t flow_##field##_show(struct elevator_queue *e, char *page)	\
+{										\
+	struct flow_data *fd = e->elevator_data;				\
+	guard(spinlock_irqsave)(&fd->lock);					\
+	return sprintf(page, "%u\n", fd->field);				\
+}										\
+static ssize_t flow_##field##_store(struct elevator_queue *e,			\
+				    const char *page, size_t count)		\
+{										\
+	struct flow_data *fd = e->elevator_data;				\
+	unsigned long val;							\
+	int ret;								\
+	ret = kstrtoul(page, 10, &val);					\
+	if (ret)								\
+		return -EINVAL;							\
+	guard(spinlock_irqsave)(&fd->lock);					\
+	fd->field = val;							\
+	return count;								\
+}
+
+#define FLOW_SYSFS_U16_RW(field)						\
+static ssize_t flow_##field##_show(struct elevator_queue *e, char *page)	\
+{										\
+	struct flow_data *fd = e->elevator_data;				\
+	guard(spinlock_irqsave)(&fd->lock);					\
+	return sprintf(page, "%u\n", (unsigned int)fd->field);			\
+}										\
+static ssize_t flow_##field##_store(struct elevator_queue *e,			\
+				    const char *page, size_t count)		\
+{										\
+	struct flow_data *fd = e->elevator_data;				\
+	unsigned long val;							\
+	int ret;								\
+	ret = kstrtoul(page, 10, &val);					\
+	if (ret || val > U16_MAX)						\
+		return -EINVAL;							\
+	guard(spinlock_irqsave)(&fd->lock);					\
+	fd->field = (u16)val;							\
+	return count;								\
+}
+
+#define FLOW_SYSFS_U64_RW(field)						\
+static ssize_t flow_##field##_show(struct elevator_queue *e, char *page)	\
+{										\
+	struct flow_data *fd = e->elevator_data;				\
+	guard(spinlock_irqsave)(&fd->lock);					\
+	return sprintf(page, "%llu\n", fd->field);				\
+}										\
+static ssize_t flow_##field##_store(struct elevator_queue *e,			\
+				    const char *page, size_t count)		\
+{										\
+	struct flow_data *fd = e->elevator_data;				\
+	unsigned long long val;							\
+	int ret;								\
+	ret = kstrtoull(page, 10, &val);					\
+	if (ret)								\
+		return -EINVAL;							\
+	guard(spinlock_irqsave)(&fd->lock);					\
+	fd->field = val;							\
+	return count;								\
+}
+
+FLOW_SYSFS_S32_RW(sync_budget_sectors)
+FLOW_SYSFS_S32_RW(async_budget_sectors)
+FLOW_SYSFS_U16_RW(batch_max_read)
+FLOW_SYSFS_U16_RW(batch_max_write)
+FLOW_SYSFS_U64_RW(completion_window_ns)
+
+/* starvation_max is an array, not a scalar field — need custom accessors */
+#define FLOW_STARVATION_ATTR_RW(lane, field_suffix, lane_idx)		\
+static ssize_t flow_starvation_max_##field_suffix##_show(		\
+	struct elevator_queue *e, char *page)				\
+{									\
+	struct flow_data *fd = e->elevator_data;			\
+	guard(spinlock_irqsave)(&fd->lock);				\
+	return sprintf(page, "%u\n", fd->starvation_max[lane_idx]);	\
+}									\
+static ssize_t flow_starvation_max_##field_suffix##_store(		\
+	struct elevator_queue *e, const char *page, size_t count)	\
+{									\
+	struct flow_data *fd = e->elevator_data;			\
+	unsigned long val;						\
+	int ret;							\
+	ret = kstrtoul(page, 10, &val);					\
+	if (ret)							\
+		return -EINVAL;						\
+	guard(spinlock_irqsave)(&fd->lock);				\
+	fd->starvation_max[lane_idx] = (u32)val;			\
+	return count;							\
+}
+
+FLOW_STARVATION_ATTR_RW(reserved, reserved, FLOW_LANE_RESERVED)
+FLOW_STARVATION_ATTR_RW(shared, shared, FLOW_LANE_SHARED)
+FLOW_STARVATION_ATTR_RW(contained, contained, FLOW_LANE_CONTAINED)
+
+FLOW_SYSFS_U32_RW(contain_threshold)
+FLOW_SYSFS_U32_RW(contain_decay_step)
+
+/* ── sysfs attribute table ────────────────────────────────────────── */
+
+#define FLOW_ATTR_RW(name) __ATTR(name, 0644, flow_##name##_show,	\
+				  flow_##name##_store)
+#define FLOW_ATTR_RO(name) __ATTR(name, 0444, flow_##name##_show, NULL)
+
+static struct elv_fs_entry flow_sched_attrs[] = {
+	FLOW_ATTR_RO(version),
+	FLOW_ATTR_RW(read_priority),
+	FLOW_ATTR_RW(sync_budget_sectors),
+	FLOW_ATTR_RW(async_budget_sectors),
+	FLOW_ATTR_RW(batch_max_read),
+	FLOW_ATTR_RW(batch_max_write),
+	FLOW_ATTR_RW(completion_window_ns),
+	FLOW_ATTR_RW(starvation_max_reserved),
+	FLOW_ATTR_RW(starvation_max_shared),
+	FLOW_ATTR_RW(starvation_max_contained),
+	FLOW_ATTR_RW(contain_threshold),
+	FLOW_ATTR_RW(contain_decay_step),
+	__ATTR_NULL,
+};
+
+/* ── Elevator type declaration ────────────────────────────────────── */
+
+static struct elevator_type mq_flow = {
+	.ops = {
+		.next_request		= flow_next_request,
+		.former_request		= flow_former_request,
+		.limit_depth		= flow_limit_depth,
+		.allow_merge		= flow_allow_merge,
+		.insert_requests	= flow_insert_requests,
+		.prepare_request	= flow_prepare_request,
+		.dispatch_request	= flow_dispatch_request,
+		.completed_request	= flow_completed_request,
+		.finish_request		= flow_finish_request,
+		.has_work		= flow_has_work,
+		.init_sched		= flow_init_sched,
+		.exit_sched		= flow_exit_sched,
+		.init_hctx		= flow_init_hctx,
+		.exit_hctx		= flow_exit_hctx,
+		.init_icq		= flow_init_icq,
+		.exit_icq		= flow_exit_icq,
+	},
+	.elevator_attrs	= flow_sched_attrs,
+	.elevator_name	= "flow-iosched",
+	.elevator_owner	= THIS_MODULE,
+	.icq_size	= sizeof(struct io_cq) + sizeof(struct flow_icq_data),
+	.icq_align	= __alignof__(struct flow_icq_data),
+};
+
+MODULE_ALIAS("mq-flow-iosched");
+
+#define FLOW_PROGNAME "Multi-Lane I/O Scheduler (FLOW)"
+
+static int __init flow_init(void)
+{
+	pr_info("%s %s\n", FLOW_PROGNAME, FLOW_VERSION);
+	return elv_register(&mq_flow);
+}
+
+static void __exit flow_exit(void)
+{
+	elv_unregister(&mq_flow);
+}
+
+module_init(flow_init);
+module_exit(flow_exit);
+
+MODULE_AUTHOR("flow-iosched contributors");
+MODULE_LICENSE("GPL v2");
+MODULE_DESCRIPTION(FLOW_PROGNAME);

